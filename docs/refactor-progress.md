# Refactor Progress — SMDP Portal

Catatan perubahan refactor UI ringan yang sudah dilakukan.

## Landing Page (`src/app/page.tsx`)

- [x] Hapus teks yang tidak diperlukan: `RSUD BAHTERAMAS v1.0`.
- [x] Persingkat teks panjang seperti `Masuk ke Aplikasi`.

## Login Page (`src/modules/auth/components/LoginForm.tsx`)

- [x] Ubah teks `Masuk ke SIMPD` menjadi `SIMPD`.

## Komponen Baru

```shell
npx shadcn@latest add input-group
```
